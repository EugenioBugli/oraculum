
[workspace]
name = "tcm-field-viewer"
version = "0.1.0"
description = "RoboCup field viewer GUI"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.11"
matplotlib = ">=3.8"
pyyaml = ">=6.0"
tk = "*"

[tasks]
viewer = "python field_viewer.py"
